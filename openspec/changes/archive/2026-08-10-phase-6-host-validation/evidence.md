# Phase 6 Validation Evidence

Date: 2026-08-10

This is a redacted, local-only evidence projection for the approved
`phase-6-host-validation` batch. It contains no raw JSONL, source records,
transcripts, credentials, HEC tokens, authorization data, exact private paths,
or service-unit bytes. Temporary validation material was removed after each
dependent assertion. No Rust, rule, schema, parser, state, sink, installer,
or service implementation was changed.

## Evidence ledger

Each row uses the evidence fields required by `design.md`. `failure_class=none`
means PASS; BLOCKED rows use the environment/gate classification from the
approved decision table. `retry_count=0` for every row: no inconclusive retry
was needed.

| check_id | evidence_ref / attempt_id | evidence_class / scope | command or query shape and preconditions | result and observations / pass basis | privacy, cleanup, and failure metadata |
| --- | --- | --- | --- | --- | --- |
| 1.1 | `E6-1.1` / `p6-1.1-a1` | `ci` / release branch + PR boundary | Read-only git boundary commands, stash-presence check, PR metadata and checks query. | **PASS** — branch `release/0.5.0-maturation`; reviewed HEAD `774020f`; PR #9 Draft and bound to the reviewed head; 5/5 supplied checks completed successfully (Format, Clippy, Test, Windows, macOS). `stash_present=true`; `export_present=true` from untracked-name status only; allowed planning/status scope was limited to `.ai/` durable state and `openspec/changes/phase-6-host-validation/**`; no product diff. | Stash/export contents were not inspected or modified. `cleanup=complete`; `failure_class=none`; no gate/defect. |
| 1.2 | `E6-1.2` / `p6-1.2-a1` | `fixture` / private synthetic workspace | `umask 077`, private temporary workspace with positive, benign, failure, state, logs, service, Splunk, and evidence purposes; copied checked-in synthetic fixtures only. | **PASS** — workspace mode `0700`; eight isolated purposes; positive/benign/malformed fixture identities and redacted ledger schema initialized. | Immediate cleanup trap registered and workspace removed. `cleanup=complete`; `failure_class=none`; no gate/defect. |
| 2.1 | `E6-2.1` / `p6-2.1-a1` | `fixture` / Codex synthetic positive | Bounded write-enabled fixture scan; `jq -e`; ephemeral `jsonschema`; native constructor and JSONL/HEC parity tests. | **PASS** — exit `0`; parseable/schema-valid `4/4`; families activity `1`, detection `1`, health `1`, session-risk-summary `1`; schema `3.0`; canonical field-presence booleans all `true` for event identity, client/session, source hash, rule/category/severity/score, response, and supplied timeline anchors; expected session and rule `tool.injection.shape`; constructor `PASS`; sink parity `PASS`. Detection event-ID set hash prefix `080a67bede5710d3`. | Raw event bodies discarded after dependent checks. `cleanup=complete`; `failure_class=none`; `G-EVENT` passed; no defect handoff. |
| 2.2 | `E6-2.2` / `p6-2.2-a1` | `fixture` / diagnostic projection | Bounded projections of `source_processing`, `detection_flow`, `runtime`, `effective_configuration`, `source_discovery`, and `diagnostic_warnings`; Event 3.0 field-presence assertions. | **PASS** — all six diagnostic sections present; warning-code aggregate `1`; health `1`, scanner errors `0`, operational alerts `0`, detections `1`; no-triage `true`; no-raw-content `true`; no `triage`, `llm_triage`, `adr_version`, model/guard verdict, or native triage transport artifact. | Only counts, warning categories, and booleans retained; projected stdout deleted. `cleanup=complete`; `failure_class=none`; `G-EVENT` and privacy boundary passed. |
| 3.1 | `E6-3.1` / `p6-3.1-a1` | `fixture` / separate scan processes | Same positive root and state path across separate bounded scans; projected state metadata only. | **PASS** — exits `0/0`; first/second emitted detections `1/0`; second deduplicated detections `1`; state commit `true`; state schema `1.0`; unique detection-ID count `1`; state hash prefix `b2728b329e281acb`. | Synthetic source only; state contents and source fingerprints not retained. `cleanup=complete`; `failure_class=none`; `G-DEDUP` passed. |
| 3.2 | `E6-3.2` / `p6-3.2-a1` | `fixture` / benign versus malformed synthetic source | Separate bounded scans of the checked-in benign fixture and named truncated synthetic JSONL input. | **PASS** — benign exit `0`: parsed/failed/empty `1/0/0`, candidates/emitted `0/0`, warning aggregate `2`, scanner errors `0`, state commit `true`; malformed exit `0`: parsed/failed/empty `0/1/0`, candidates/emitted `0/0`, warning aggregate `3`, scanner-error category count `1`, state commit `true`. Source outcomes are observably distinct. | Error evidence projected to category only; malformed/benign roots and logs deleted. `cleanup=complete`; `failure_class=none`; `G-EVENT` passed. |
| 4.1 | `E6-4.1` / `p6-4.1-a1` | `fixture` / controlled file-source continuity | One synthetic source with initial metadata line, unchanged scan, then one controlled appended assistant-event line. | **PASS** — source counts `1/1/1`; parsed records `1/1/2`; emitted detections `0/0/1`; deduplicated detections `0/0/0`; changed-source count `1`; cursor/fingerprint present `true`; final pass contains expected rule. Incremental detection-ID set hash prefix `92b4aa13c4ad4c21`. | Synthetic source only; no historical/local JSONL used. `cleanup=complete`; `failure_class=none`; `G-BACKFILL` passed for controlled file continuity. |
| 4.2 | `E6-4.2` / `p6-4.2-a1` | `fixture` / controlled SQLite contract | `cargo test --test cli migrated_state_preserves_sqlite_cursor_continuity`; `cargo test --test cli migrated_sqlite_cursor_emits_only_newly_appended_sessions`. | **PASS** — both approved synthetic cursor tests `PASS`; migration continuity and newly-appended-only emission were covered without opening a real database. | Temporary/test data only; no user database accessed. `cleanup=complete`; `failure_class=none`; `G-BACKFILL` passed for approved controlled SQLite coverage. |
| 5.1 | `E6-5.1` / `p6-5.1-a1` | `fixture` / Linux synthetic rotation | Exact 500-file scan twice with threshold `1000` and `keep=3`; parse retained JSONL; concurrency rotation test; ignored Linux multi-cycle soak. | **PASS** — active/rotated file counts `1/1`; retained parseable lines `1001/1001`; duplicate event IDs `0`; first/second detections `500/0`; state separation `true`; first-run families activity `500`, detection `500`, health `1`; restart emitted no replay; rotation, concurrency, and Linux soak `PASS`. Retained-ID set hash prefix `2931a15d2e98ac42`. | Temporary JSONL/state removed; real telemetry path unchanged. `cleanup=complete`; `failure_class=none`; `G-ROTATION` passed for this Linux synthetic path. |
| 6.1 | `E6-6.1` / `p6-6.1-a1` | `host` / redacted Linux host; `codex`, `opencode` | Metadata-only source-root checks, then `timeout 120s` bounded dry-run with `--client`, `--max-sources 5`, actual host root, and no fixture-write flag. | **BLOCKED** — Codex bounded result selected/returned/operational `0/0/0` with zero usable parse output; OpenCode selected/returned/operational counts unavailable because the bounded command reached `120s`. No parser defect was established and no unbounded scan was attempted. | Dry-run output discarded; no log/state target created. `cleanup=complete`; `failure_class=B`; `retry_count=0`; `release_gate_id=G-HOST-SOURCE`; `defect_handoff=none`. |
| 7.1 | `E6-7.1` / `p6-7.1-a1` | `service` / Linux user-systemd | Non-mutating manager detection and bounded unit metadata; isolated `--from-source --install-dir <temporary> --no-timer` staging attempt. | **BLOCKED** — user-systemd was reachable; existing canonical service/timer were absent and inactive; staging failure category was isolated artifact staging/load unavailable (exit `1`); no canonical staged artifacts or temporary path override became loadable. Pre-state class was absent/inactive and bounded metadata hashes matched; no activation or real-profile mutation was performed. | Override was not installed; restoration basis was no mutation plus pre-state absence/inactivity and post-cleanup verification. Temporary staging removed. `cleanup=complete`; `failure_class=B`; `retry_count=0`; `release_gate_id=G-SERVICE`; `defect_handoff=none`. |
| 7.2 | `E6-7.2` / `p6-7.2-a1` | `service` / Linux user-systemd | Canonical repeated execution and timer/reload checks, conditional on safe isolated staging. | **BLOCKED** — precondition 7.1 isolation failed, so service/timer execution was not attempted and no service claim is made. | No temporary unit or timer remained; no restoration was required beyond confirming no mutation. `cleanup=complete`; `failure_class=B`; `retry_count=0`; `release_gate_id=G-SERVICE`; `defect_handoff=none`. |
| 8.1 | `E6-8.1` / `p6-8.1-a1` | `splunk` / live HEC precondition | Allowlisted presence/status check for approved endpoint and token references; no values read or printed. | **BLOCKED** — no complete operator-approved endpoint plus secret reference was available; target tuple `index=telltale`, `sourcetype=telltale:json`, `source=telltale` was not exercised; HEC delivery status, sink-failure count, local-vs-HEC event-ID comparison, and repeat-scan delivery were not applicable. No HEC request or controlled event delivery was attempted. | No temporary HEC config or secret file was created; no server mutation. `cleanup=complete`; `failure_class=B`; `retry_count=0`; `release_gate_id=G-HEC`; `defect_handoff=none`. |
| 8.2 | `E6-8.2` / `ses_010b39d49ffe12l1I1Q45v4nMR` | `splunk` / delegated analyst, read-only | `splunk-analyst` used only bounded canonical-tuple searches with `_raw` excluded and row limits at or below `100`; no mutation calls. The 8.1 precondition was not met, so no controlled event IDs or HEC window were available; analyst used a bounded read-only access/searchability window of no more than `10` minutes. | **BLOCKED** — read-only access was available, but no controlled Event 3.0 events matched `index=telltale`, `sourcetype="telltale:json"`, `source=telltale`; controlled detection lookup, field extraction, deduplication, health separation, and process-chain proof were therefore not exercised. No HEC delivery claim is made. | Only canonical tuple status/counts were retained; unrelated historical metadata from the analyst response was discarded. `cleanup=complete`; `failure_class=B`; `retry_count=0`; `release_gate_id=G-SPLUNK`; `defect_handoff=none`; no-mutation confirmation recorded. |
| 9.1 | `E6-9.1` / `p6-9.1-a1` | `release_gate` / evidence safety | `git diff --check`, allowed-path/status review, and bounded value-pattern scan over this projected file only. | **PASS** — retained evidence is limited to redacted counts, statuses, field names/booleans, synthetic hash prefixes, and gate metadata; no forbidden credential/path-value patterns matched. Allowed-path list: `.ai/working-state.md`, `.ai/task-queue.md`, `PLAN.md`, and `openspec/changes/phase-6-host-validation/**`; `docs/release-readiness.md` was not changed. `stash_present=true`; `export_present=true` from status metadata only. For validation rows 1.1–8.2: PASS `9`, BLOCKED `5`, FAIL `0`; all rows have references and cleanup/failure metadata. | Raw temporary outputs, state, service staging, HEC references, fixture copies, and analyst handoff material were removed or not retained. Export/stash were not scanned or touched. `cleanup=complete`; `failure_class=none`; `G-PRIVACY` passed. |
| 9.2 | `E6-9.2` / `p6-9.2-a1` | `release_gate` / final measured matrix | Ledger reconciliation against all tasks 1.1–8.2; CI-only boundary and unexercised-gate review. | **PASS** — every executed validation item has an explicit PASS or BLOCKED disposition; blocked rows name the gate and B classification; PR #9 remains Draft; supplied CI is labeled CI only; native Windows/macOS host/service, publication/tagging, packaging/preflight, hosted-site, HEC, and controlled Splunk gates are not overstated. | No public/release claim or raw operational evidence added. `cleanup=complete`; `failure_class=none`; `G-COMPLETION` remains pending only for final archive bookkeeping. |
| 9.3 | `E6-9.3` / `ses_010a9ca48ffet3yXcpCTVPUKeS` | `release_gate` / independent Luna Max evidence review | Fresh `coder-quality` review of the approved artifacts, redacted ledger, measured statuses, cleanup, privacy boundary, and current repository scope; no secrets, raw source/Splunk content, stash, or export shared. | **PASS after correction** — the independent review initially identified missing minimum counters, stale durable status, and incomplete blocked-row detail. Those concrete evidence/documentation findings were corrected in `evidence.md`, `PLAN.md`, and local durable state; no product change or defect repair was requested, and no unsupported platform/transport claim remains. | Review inputs remained redacted; no raw telemetry or credentials retained. `cleanup=complete`; `failure_class=none`; no defect handoff; `G-COMPLETION` remains pending only for final archive bookkeeping. |
| 9.4 | `E6-9.4` / `p6-9.4-a1` | `release_gate` / final repository and archive boundary | `openspec validate --changes phase-6-host-validation` `PASS`; `cargo fmt --all --check`, `cargo clippy --all-targets -- -D warnings`, and `cargo test` `PASS`; `git diff --check`, allowed-path review, PR Draft/head reconfirmation, archive move, `openspec list --json`, commit, and normal push. | **PASS** — all 18 task dispositions are explicit; archive is `2026-08-10-phase-6-host-validation`; active OpenSpec list is empty; Rust checks passed (`211` tests, `0` failures, `1` ignored); no product implementation or sensitive evidence path entered the change; evidence/status commit `0617b4d` was pushed to `origin/release/0.5.0-maturation`. Post-push PR checks are explicitly **PENDING** (no completed runs reported), so prior green CI is not re-used as post-push proof. | README/docs stash and Tokscale export remain untouched; temporary validation/service state is absent; no tag/release/merge/ready/publication occurred. `cleanup=complete`; `failure_class=none`; `G-COMPLETION=PASS` on final commit/push and clean-status confirmation. |

## Gate matrix and measured boundary

| Gate | Result | Measured basis |
| --- | --- | --- |
| `G-EVENT` | PASS | Synthetic Event 3.0 JSONL, schema validation, diagnostics, no-triage, and sink-parity checks passed. |
| `G-DEDUP` | PASS | Separate-process state persistence and unchanged-scan detection suppression passed. |
| `G-BACKFILL` | PASS (controlled only) | File append continuity and approved synthetic SQLite cursor tests passed; no private historical backfill claim. |
| `G-ROTATION` | PASS (Linux synthetic only) | Exact 500-file rotation/restart, parseability, duplicate-ID, concurrency, and ignored Linux soak checks passed. |
| `G-HOST-SOURCE` | **BLOCKED** | Codex bounded discovery returned zero usable sources; OpenCode hit the timeout. |
| `G-SERVICE` | **BLOCKED** | Linux user-systemd was detected, but safe isolated staging could not be loaded; execution was not attempted. |
| `G-HEC` | **BLOCKED** | No approved endpoint/token reference was available without reading secrets. |
| `G-SPLUNK` | **BLOCKED** | Read-only analyst access worked, but the canonical tuple contained no controlled Phase 6 events. |
| `G-PRIVACY` | PASS | Evidence is projected to counts, statuses, field names/booleans, synthetic hashes, and redacted classes; temporary material was removed. |
| `G-COMPLETION` | PASS | Final task state, archive move, repository checks, status alignment, normal push, and clean-boundary confirmation completed; post-push CI remains explicitly pending; no release/publication action is included. |

The supplied PR #9 Linux, Windows, and macOS checks are CI evidence only. This
batch does not claim native Windows or macOS host/source/service validation,
publication/tagging, `make release-preflight`, packaging/publication, hosted
site cutover, or any other unexercised release gate. No follow-up defect change
is created in this session; the blocked environmental gates are recorded for a
separate bounded decision.

## Evidence sanitization

The retained file was reviewed with a bounded forbidden-material check before
status updates. It contains no secret-like values, credentials, HEC tokens,
authorization headers, `.env` values, raw source/transcript/session content,
raw Splunk responses, exact private paths, service-unit bytes, stash content, or
Tokscale export content. Temporary raw logs, state, fixture copies, service
staging, and secret-reference material were absent after cleanup.
